# Normalize line endings to LF in the repository so contributors on Windows,
# macOS and Linux do not produce whole-file diffs.
* text=auto eol=lf

# Dataset files are read line by line; keep them LF too.
*.txt text eol=lf
*.dict text eol=lf
*.csv text eol=lf
